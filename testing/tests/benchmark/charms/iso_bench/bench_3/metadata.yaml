name: iso-bench-3
summary: Benchmark charm 3 for persistent-worker isolation benchmarking.
description: |
  Synthetic charm used by the isolation benchmark. It performs a
  modest, representative reconcile (read config, set workload version, set unit
  status) on each of several lifecycle events.
